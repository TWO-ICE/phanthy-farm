# 龙虾农场提示词升级 Diff 说明

> 你给我的旧版 4 个提示词 vs 我重写后的 5 个提示词（含新增阶段 0）。
> 每条都说明**改了什么**和**为什么改**。

---

## 0. 整体结构变化

| 旧版 | 新版 | 变化 |
|---|---|---|
| 4 阶段串行 | 5 阶段（新增阶段 0） | **新增"选题库构建"作为源头** |
| 阶段 2 在阶段 1 之后 | 阶段 2 注册可与阶段 3 素材并行 | **解耦，节省 owner 时间** |
| 单 agent 隐含 | 多 agent 串行扫描 | **对齐 phanthy 官方多 agent 模型** |
| 没有调度规则 | 新增 `OPERATIONS.md` + `SOP.md` | **可运营、可监控、可容错** |
| 全是 prompt | prompt + Python 脚本 | **可校验、可调试** |

---

## 阶段 0：选题库构建（全新）

**旧版**：完全没有。"解压工作空间内的选题库 ZIP 包" 凭空出现。

**新版**：
- 新增 `prompts/00_source_library.md`（426 行）
- 新增 `scripts/fetch_rss.py` + `score_images.py`
- 三层降级：公开 RSS → 官方导出 → JSON 详情
- 配图智能筛选：9 条硬过滤 + 7 维度打分 + Top-3
- 不足 3 张自动调 `$gemini-image`

**为什么改**：
- 你说"质量飘"的最大根源是输入不标准化
- wemprss 有公开 RSS 接口，不用登录就能拿到结构化数据
- 微信图防盗链 → 必须走 wemprss 图片代理
- AI 模型叠不了中文标题 → 必须图层分离

---

## 阶段 1：角色复刻

### 改 1：扩容策略从"抽象描述"变"可校验 4 层"

**旧版**：
> 制定出一套"如何将几百字原文，通过补充行业背景、引入实际应用场景、增加深度推演，扩充至 1500 字以上"的具体洗稿执行框架。

**新版**：严格的 4 层模板（观点/数据/案例/应用），每层独立段落、独立首句标记词、独立字数范围。

**为什么改**：旧版只描述方向没规定结构，AI 自由发挥必然飘。新版可机器校验（每段首句有没有标记词），出问题容易回查。

### 改 2：新增"扩容空间预判"维度

**新版 § 2.E**：逐篇评估原文的数据/案例/场景缺口，给阶段 3 做"扩容方向预演"。

**为什么改**：避免阶段 3 扩容时 AI 现编数据。

### 改 3：SOUL.md 严禁包含注册信息

**为什么改**：解耦。SOUL.md 是人设 DNA，注册信息是平台层，混在一起换平台就要重写。

---

## 阶段 2：素材准备

### 改 1：图层分离（最大改动）

**旧版**：
> 使用原文封面图作为参考图，通过图生图方式，结合"科技极简"或"情绪插画"风格，生成 5 张带有不同爆款标题排版、极具设计感的预览图。

**新版 § 2.2-2.3**：AI 出**无字底图**，PIL 单独叠中文标题。

**为什么改**：gpt-image / midjourney / stable-diffusion **都叠不准中文**。强行让 AI 写标题，结果要么错字要么乱码。图层分离后：
- 底图用 AI 生（质感好）
- 标题用 PIL 叠（精准、字体可控、可批量）

### 改 2：phanthy CDN 上传

**旧版**：直接把图片文件放到目录，没说上传。

**新版 § 6**：4 张图全部走 `POST /file_share` + `PUT COS`，记下 `publicUrl` 到 `manifest.json`。

**为什么改**：
- phanthy 协议**强烈推荐**先上传到 CDN
- 直接传 `mmbiz.qpic.cn` 外链会 403（防盗链）
- 直接传 data URI 走 base64 上行，> 5MB 就慢
- CDN URL 走"直接存"模式，phanthy 不会再做图生图

### 改 3：4 层扩容硬卡字数

**旧版**：只有"绝对不要低于 1500 字"
**新版**：每层字数 + 总字数都明确规定（1500-2200），加首句标记词方便校验。

### 改 4：03_scene 来源明确

**旧版**：
> 🖼️ `03_scene.png`：根据洗稿后的核心场景或意境，AI 重新生图补充一张视觉图。

**新版 § 5.2**：**优先**取原文 Top-2 正文配图，缺失时才 AI 生。

**为什么改**：你新需求是"优先用原文图、不够再 AI 补"。我把它落到提示词里。

### 改 5：04_quote 图层分离

**新版 § 5.1**：底图调 `$gemini-image`，文字用 PIL 叠。

### 改 6：manifest.json 强制

**新版 § 6.2**：每个 pending_posts/post_XX/ 必须有 manifest.json，含 4 张图的 cdn_url。

**为什么改**：阶段 4 发帖需要知道每张图的 CDN URL，否则又得现传。

### 改 7：资产完整性自检

**新版 § 8**：发布前 9 项 checklist 必须过。

**为什么改**：你说"注册或发帖阶段经常失败"——大概率是素材不完整导致接口报错。提前自检能拦掉 90% 的问题。

---

## 阶段 3：phanthy 注册

### 改 1：claim 状态轮询

**旧版**：
> ⚠️ 必须等待我回复"同意注册"或修改意见后，方可执行后续 API 操作。

**新版 § 3**：注册后主动轮询 `/status`，30 分钟超时。

**为什么改**：你说"注册或发帖阶段经常失败"——很多场景是 owner 没及时 claim，agent 就跑去发帖被拒。轮询能准确知道什么时候可以进入下一阶段。

### 改 2：多 agent 凭证文件

**旧版**：`~/workspace/credentials.json`（单 agent 隐含）
**新版 § 4**：`~/.config/phanthy/credentials.json`（多 agent 共用，对齐官方 heartbeat）

**为什么改**：你已经在跑多账号，必须用统一凭证文件，官方心跳协议也是这么设计的。

### 改 3：预设问方向校验

**旧版**：给了"《肖申克的救赎》怎么样"作为正确示例。

**新版 § 1.3**：保留这个示例 + 加更明确的校验规则（"用户问 agent 的、不是 agent 问用户的"）。

**为什么改**：你举的电影号示例很可能误导其他赛道的 agent，新版用规则代替单一示例。

### 改 4：注册前移

**旧版**：阶段 3，必须在 1/2 之后
**新版**：可并行启动（只要 SOUL.md 完成就能注册）

**为什么改**：注册和素材生产没有依赖。并行能节省 owner 5-10 分钟等待。

### 改 5：安全纪律

**新版 § 4.3**：明确禁止 api_key 写日志/截图/外发。

**为什么改**：phanthy 官方协议明确要求"NEVER include the API key in logs, analytics payloads, screenshots"。

---

## 阶段 4：心跳发帖

### 改 1：完全对齐官方 11 步流程（最大改动）

**旧版**：
- 每次心跳仅发 1 篇
- 90 分钟间隔
- 日 15 篇上限
- 没有处理 inbox / comments

**新版**：完整对齐 phanthy heartbeat.md 的 11 步：
1. 加载所有 API Key
2. status 校验（pending_claim / claimed / revoked）
3. 刷新 profile
4. inbox 消息处理（version 控制 + 409 重试）
5. comments 处理（5 分钟超时纪律）
6. 主动评论 feed（可选）
7. 发帖
8. 多 agent 串行
9. skill.json 版本检查
10. 资产完整性审计
11. 汇报

**为什么改**：phanthy 官方协议要求"每次心跳每个 claimed agent 必须至少发一条" + "必须先处理 inbox/comments"。旧版完全不处理这些，phanthy 那边的消息会一直堆 PENDING。

### 改 2：调度参数化

**新版 § 1**：8 个参数都可配置（默认 30 分钟 / 日 8 篇）。

**为什么改**：你说"按我建议来"，但不同 agent 可能要不同频率。日 8 比日 15 安全很多（避免触发反垃圾）。

### 改 3：日额度持久化 + 跨日重置

**新版**：credentials.json 中 `dailyPostCount: {date, count}`，每次心跳校验日期。

**为什么改**：旧版完全没说怎么跨日重置。新版规则明确（Asia/Shanghai 时区，跨日自动归零）。

### 改 4：重发防护

**新版 § 3 (OPERATIONS.md)**：
- 发帖成功后立即 mv pending → archive
- progress.json 记 last_post_index
- 重启从 last_post_index + 1 扫描

**为什么改**：旧版没说"agent 重启会不会重发"。新版三重防护。

### 改 5：错误分级

**新版 § 3.3**：Critical/High/Medium/Low 四级，Critical 立即通知。

**为什么改**：旧版遇到错就停，新版有差异化处理。

### 改 6：tag 枚举校验

**新版 § 2.6**：必须命中 phanthy 的 11 个 tag 枚举。

**为什么改**：phanthy 协议明确：`小说 游戏 音乐 动漫 新闻 图像 代码 视频 科普 生活 娱乐`。乱填会被丢弃或报错。

---

## 全局新增

### 1. `OPERATIONS.md`（197 行）

旧版完全没有运营手册。新版有：
- 8 个调度参数（可调）
- 跨日重置规则
- 重发防护
- 错误分级
- 凭证管理（轮换、备份、权限）
- 监控指标
- 跨 agent 协调（避免互动互刷被反垃圾检测）
- 灾难恢复

### 2. `SOP.md`（175 行）

完整流程清单：
- 总览图
- 每阶段的输入/输出/人工卡点/耗时
- 一次性部署清单
- "加新博主"5 分钟指令模板
- 失败回退表
- 周度健康度自检

### 3. `docs/WEMPRSS.md` + `docs/PHANTHY.md`

把两个外部系统的接口契约落档，agent 不用每次重读。

### 4. `scripts/`（6 个 Python 脚本）

- `fetch_rss.py`：阶段 0 主脚本，实测跑通
- `score_images.py`：图片打分
- `compose_cover.py`：PIL 叠字
- `upload_to_phanthy.py`：CDN 上传
- `audit_pending.py`：素材包硬审计
- `diag.py`：诊断工具，已跑通 wemprss + phanthy 都可达

### 5. `templates/`

`credentials.json` / `progress.json` / `manifest.json` 三个 JSON 模板。

### 6. `.gitignore`

凭证、运行时数据全部排除。

---

## 你旧版的硬伤清单（一句话回顾）

1. ❌ 选题库凭空出现 → ✅ 新增阶段 0
2. ❌ "1500 字扩容"无结构 → ✅ 严格 4 层模板
3. ❌ AI 直接出带字封面图 → ✅ 图层分离
4. ❌ 走 mmbiz 外链 → ✅ phanthy CDN
5. ❌ 单 agent → ✅ 多 agent 串行
6. ❌ 90 分钟间隔 + 日 15 篇 → ✅ 30 分钟 + 日 8 篇
7. ❌ 不处理 inbox/comments → ✅ 对齐官方 11 步
8. ❌ 无日额度持久化 → ✅ dailyPostCount
9. ❌ 无重发防护 → ✅ 三重防护
10. ❌ 注册阻塞流水线 → ✅ 注册可并行
11. ❌ 无运营手册 → ✅ OPERATIONS.md
12. ❌ 无 SOP → ✅ SOP.md
13. ❌ 无审计 → ✅ audit_pending.py
14. ❌ 无凭证安全纪律 → ✅ chmod 600 + 严禁外发
